---
const { number, title, text } = Astro.props;
---

<div class="container">
  <div class="headersplit">
    <span class="number">{number}</span>
    <h1 class="heading">{title}</h1>
  </div>
  <p class="content">
    {text}
  </p>
</div>

<style>
  .container {
    max-width: 600px; /* Adjust width as needed */
    margin: 0 auto;
    font-family: Arial, sans-serif;
    padding: var(--spacing-text-padding-vertical, 32px)
      var(--spacing-text-padding-horizontal, 48px);
  }

  .headersplit {
    display: flex;
    align-items: center; /* This will vertically center the items in the container */
    gap: 1rem; /* This adds space between the items */
  }

  .number {
    color: var(--color-complimentary-pine-green-dark, #142c2b);
    font-family: Rubik;
    font-size: 100px;
    font-style: normal;
    font-weight: 700;
    line-height: normal;
  }
  .heading {
    color: var(--color-complimentary-pine-green-dark, #142c2b);
    font-family: Rubik;
    font-size: 32px;
    font-style: normal;
    font-weight: 700;
    line-height: normal;
  }
  .content {
    color: var(--color-complimentary-pine-green-medium, #2a5f5e);
    /* Paragraph20Medium */
    font-family: Rubik;
    font-size: 20px;
    font-style: normal;
    font-weight: 500;
    line-height: 150%; /* 30px */
  }
</style>
