---
const { image, title, description } = Astro.props;
---

<section class="oneadvice">
  <div>
    <img src={image} alt="" />
  </div>
  <div>
    <h3>{title}</h3>
    <p>{description}</p>
    <style>
      .oneadvice {
        display: flex;
        align-items: center;
        /* justify-content: left; */
        gap: 1.75rem;
        width: 500px;
      }

      p {
        font-size: 20px;
      }
      h3 {
        font-weight: 600;
      }
    </style>
  </div>
</section>
